# CNC Shield V3 接線總表

對應韌體：`src/main.cpp`

---

## 一、已完成

| Shield 位置 | 腳位 | 接什麼 | 狀態 |
|---|---|---|---|
| X 槽 | D2 / D5 | A4988 → 相機 X 軸馬達 | ✅ |
| Y 槽 | D3 / D6 | A4988 → 相機 Y 軸馬達 | ✅ |
| — | D8 | EN 致能（Shield 內建走線，三軸共用） | ✅ |
| X/Y 微步 | — | MS1 插、MS2 空、MS3 插 = 1/2 微步 | ✅ 不要動 |
| `12-36V` 端子 | — | 馬達電源 | ⚠️ 通電前先量短路 |

> 微步維持現狀，因為 `STEPS_PER_COL = 1360` / `STEPS_PER_ROW = 1040` 是在這個
> 設定下實測出來的。動跳線帽就要重量這兩個值。

---

## 二、DM542 輸送台（待接）

用左側引出排針 **`Z.STEP/DIR`** 那一列，加最下面 `5V/GND`：

| Shield | 腳位 | → | DM542 | 備註 |
|---|---|---|---|---|
| `5V` | — | → | `PUL+` `DIR+` | 兩條並接同一點 |
| `Z.STEP` | D4 | → | `PUL-` | 脈衝 |
| `Z.DIR` | D7 | → | `DIR-` | 方向 |

- **Z 槽保持空的**，不可插 A4988（會搶訊號）
- DM542 馬達電源獨立供電，**不要和 Arduino 共地**（共地會失去光耦隔離的意義）
- DIP 開關 `SW5-8 = off/off/on/on` → 1600 脈衝/圈（2026-09-16 對照驅動器 DIP 表確認）

### 為什麼是共陽極接法

`PUL+` / `PUL-` 是 DM542 內部光耦那顆 LED 的兩隻腳。5V 接陽極端、Arduino 接陰極端，
所以腳位拉 LOW 才有電位差、LED 才導通。這就是 `src/hal/Dm542Driver.h` 裡
`pulseHigh()` 實際輸出 LOW 的原因 —— 函式名講的是邏輯層的「脈衝有效」。

---

## 三、限位開關（待接）

六顆開關，每軸兩端。X/Y 的遠端側特意不走 `END STOPS`，改接 A 槽的 D12/D13。

### 原點側 —— 右側 `END STOPS`

絲印由上到下 `Z+ Z- Y+ Y- X+ X-`。同組上下兩端子並聯到同一支腳，接哪個都一樣。

| 用途 | 接端子 | 腳位 | 程式常數 | 目前設定 |
|---|---|---|---|---|
| 相機 X 原點（左） | `X+` | D9 | `X_LIMIT_ACTIVE_LOW` | `false`（NC） |
| 相機 Y 原點（上） | `Y+` | D10 | `Y_LIMIT_ACTIVE_LOW` | `false`（NC） |
| 輸送台**頂部**＝原點 | `Z+` | D11 | `CONV_TOP_ACTIVE_LOW` | `false`（NC） |

> 同組的 `X-` / `X+` 並聯到同一支腳，接哪個都通。實際採用 `+` 端子。

### 遠端側 —— D12 / D13

這兩支腳在 Shield 上**各有兩個等效接點**（實測確認，銅箔同一條）：

| 用途 | 接點（擇一） | 腳位 | 程式常數 | 目前設定 |
|---|---|---|---|---|
| 相機 X 遠端（右） | `A.STP`（左下）或 `SpnEn`（右下） | D12 | `X_LIMIT_FAR_ACTIVE_LOW` | `false`（NC） |
| 相機 Y 遠端（下） | `A.DIR`（左下）或 `SpnDir`（右下） | D13 | `Y_LIMIT_FAR_ACTIVE_LOW` | `false`（NC） |

會有兩個名字，是因為 GRBL 對這兩支腳定義的是主軸控制（Spindle Enable /
Direction），而 Shield 又把它們引到 A 軸位置供第四軸使用。不跑 GRBL 的話
兩個名稱都沒意義，就是兩個可插線的孔。

> **建議用 `SpnEn` / `SpnDir`** —— 與 `END STOPS`、`Abort`/`Hold`/`Resume`/
> `CoolEn` 同在板子右側，九條訊號線可以集中走一邊，不與左側 DM542 的線交錯。

> A 槽那組跳線帽（標 `X` `Y` `Z` 三對針）**必須保持不插**，否則 D12/D13 會被
> 拿去複製其他軸的 STEP/DIR，與限位開關訊號衝突。

### 為什麼遠端不接 END STOPS

Shield 把 `X-` 和 `X+` 並聯到 D9，兩顆開關共用一支腳，程式分不出是哪一顆。
平常防撞沒差（壓到就停），但**開機那一刻**會出問題：`Homing` 的第一步
（`lib/domain/Homing.h:42`）假設「訊號有效 = 壓在原點側」，會往反方向退開 ——
如果實際壓的是遠端那顆，等於朝牆上再推 200 步。

拉到 D12/D13 之後兩端完全分得開，程式在 `src/main.cpp:218` 會擋下來報
`CAM_ON_FAR_LIMIT`，要求人工推離再開始。

輸送台不受影響 —— 它的頂部在 D11、底部在 A0，本來就是兩支腳。

---

## 四、感測器（待接）

右側下方那排：

| Shield 絲印 | 腳位 | 用途 | 程式常數 | 目前設定 |
|---|---|---|---|---|
| `Abort` | A0 | 輸送台**底部**限位 | `CONV_BOTTOM_ACTIVE_LOW` | `false`（NC） |
| `Hold` | A1 | 樣本到位偵測 | `SAMPLE_PRESENT_ACTIVE_LOW` | `true` |
| `Resume` | A2 | 紅外線感測器 | `IR_ACTIVE_LOW` | `true` |
| `CoolEn` | A3 | 啟動開關 | `START_SW_ACTIVE_LOW` | `true` |

> A0-A5 不是只能類比。它們在晶片上是 PORTC 的 PC0-PC5，是完整的數位 I/O，
> 只是額外接了 ADC。程式走的是 `pinMode(INPUT_PULLUP)` + `digitalRead()`，
> 完全沒碰到類比功能。

---

## 五、接線方式

**限位開關（兩條線，六顆統一 NC 接法）**

微動開關三隻腳 `C` / `NO` / `NC`，只用其中兩隻：

```
開關 C 腳  → GND
開關 NC 腳 → 訊號腳位      ← 注意是 NC，不是 NO
```

程式已開內部上拉（`src/hal/ArduinoInputPin.h:14`），**不用外接電阻**。

GND 就近接，不必全部串成一條：

- **接在 `END STOPS` 的**（X+ / Y+ / Z+）——那排是雙排針，每一列訊號旁邊
  就有自己的 GND 孔，各自接自己那列即可。
- **其餘的**（`SpnEn` / `SpnDir` / `Resume` / `Abort`）——就近找 `5V/GND`
  或 `EN/GND` 那列的 GND 孔。

**不要接左下角 `12-36V` 端子的 `-`** —— 那是馬達電源的地，大電流經過會干擾訊號。

### 為什麼統一用 NC

NC 平常是導通的，斷線 / 接觸不良時看起來像「被觸發」，程式會停機；
NO 斷線時看起來像「沒壓到」，程式會繼續推直到撞機。
失效往安全方向倒，所以六顆全部用 NC。

**三線式感測器模組**

```
VCC → 5V
GND → GND
OUT → 對應腳位
```

---

## 六、不可佔用

| 腳位 | 原因 |
|---|---|
| D0 / D1 | Serial，與 Python 通訊 |

---

## 七、待確認事項

| # | 項目 | 怎麼查 |
|---|---|---|
| 1 | `12-36V` 端子有無短路 | 通電前量兩極電阻，應 >幾百 kΩ |

### 已確認

**2026-09-21 · A1／A2 接上，流程全通**

- A1 樣本偵測、A2 拍照位置到位，都用**兩線 NC** 接法（與其他六顆限位一致），
  實測極性後 `ACTIVE_LOW` 由 `true` 改成 `false`
- 模擬模式關閉，改吃實體訊號；完整流程連跑三輪無錯誤

**測試開關已集中到 `src/TestConfig.h`**

出機前只要看那一個檔，全部 `0` 就是正式設定。目前尚未歸零的：

| 開關 | 現值 | 何時改 |
|---|---|---|
| `TEST_FAKE_CAPTURE` | 1 | Python 接上後改 0 |
| `TEST_HOMING_EVERY_N` | 2 | 確認誤差小後放寬到 10 |

**A1 誤觸的處理**

推下去若沒碰到 A2 就到底（A1 誤觸／樣本掉落／卡在半路）：記一筆
`WARN:NO_SAMPLE_AT_PHOTO` 與累計次數、`roundCount` 不加、強制歸位一次，
然後照常接下一個，不停機。累計次數持續增加就代表 A1 位置需要調整。

> 漏拍那趟 `roundCount` 不變，所以不能用「每 N 輪」判斷歸位 —— 同一個數字
> 會每趟都成立或都不成立。改用獨立旗標強制歸位。

**微步結論：維持 1/4（800 PPR）**

1/8（1600）比較安靜，但 375 rpm 需要半週期 50us，而 `loop()` 一輪
（9 個 Button + 3 個 Stepper + camera.poll）就吃掉數十 us —— UNO 16MHz
跟不上，實際轉速反而下降。

**待辦**

1. 軌道裝上機台後，九顆開關重驗、X/Y 與輸送台行程重量、`PHOTO_X/Y_STEPS` 重算
2. Python 拍照端：收 `CAPTURE:<row>,<col>` → 拍一張 → 回 `OK`


**2026-09-18 · 流程並行化 + 模擬模式**

三顆馬達是三個獨立的 `Stepper`，`loop()` 每輪都會推進各自的 `tick()` ——
本來就能同時跑，之前只是狀態機刻意排隊。改成並行後：

| 動作 | 原本 | 現在 |
|---|---|---|
| 開機歸位 | X → Y → 輸送台 | **X/Y 同時** → 輸送台 |
| 移到拍照位置 | X 走完 → Y 走 | **X/Y 同時** |
| 掃描回程 | X 走完 → Y 走 | **X/Y + 輸送台下推 三者同時** |
| 每 N 輪重新歸位 | X → Y | **X/Y 同時** |

狀態機合併：`HomeCamX/Y → HomeCam`、`ToPhotoX/Y → ToPhoto`、`ReHomeX/Y → ReHome`。
錯誤仍分別回報 `CAM_HOME_X` / `CAM_HOME_Y`。

> 關鍵：`Scanner::isReturning()` 讓外部知道「最後一格拍完、回程已開始」，
> 輸送台據此提早下推。等 `Result::Done` 才動是來不及的 —— 那時相機已經到位。

**修正：掃描 Y 方向相反**

`Scanner` 換列時把方向寫死成 `true`，但 `CAM_Y_TOWARD_HOME = true` 代表朝
D10（原點），所以每換一列都往上跑。改成由建構子傳入 `!CAM_Y_TOWARD_HOME`。

**修正：歸位後速度沒還原**

`Homing` 結束時把速度設回 `fast_`（歸位快速值），不是巡航值 —— 第一次歸位
之後輸送台所有動作都會停在歸位速度。現在每次輸送台開始移動前都明確
`setHalfPeriodUs(CONV_HALF_US)`。

**暫行模擬設定（感測器到貨後改回 false）**

```cpp
SIMULATE_SAMPLE_ALWAYS = true   // 當作頂部永遠有樣本（A1 未接）
SIMULATE_FEED_BY_STEPS = true   // 不等 A2，改走 FEED_STEPS = 32000（行程中點）
static FakeCapture camera(300); // 假裝 PC 回應，改回 SerialCapture 即可
HOMING_EVERY_N_ROUNDS = 2       // 測試期先設 2，確認誤差後放寬
```

輸送台巡航 `CONV_HALF_US = 100`（800 PPR → 375 rpm），歸位維持慢速
（快 400us / 慢 1200us）。


**2026-09-18 · 相機 X/Y 實機量測**

| 軸 | 全行程 | 掃描需要 | 拍照起點（置中） |
|---|---|---|---|
| X | 16515 步 | 12240 步 | `PHOTO_X_STEPS = 2137` |
| Y | 12108 步 | 6240 步  | `PHOTO_Y_STEPS = 2934` |

- 四顆限位（D9/D10/D12/D13）全部正常，方向正確
- 歸位動作正常：撞到 → 退開 → 慢速再撞一次，**無跳齒**
- 按 A3 後相機停在樣本盤第一格，位置正確
- ⚠️ **X 軸餘裕僅 2137 步**（掃描用掉 74% 行程），累積誤差要靠定期歸位清掉

> 開機時若相機壓在遠端限位上會報 `FAULT:CAM_ON_FAR_LIMIT` 並拒絕歸位 ——
> 這是刻意的保護（歸位第一步假設「被壓 = 在原點側」，壓遠端會往牆上推）。
> 手動把滑塊推離限位再重開機即可。

> **皮帶傳動注意**：撞到限位時若還在高速推會跳齒。之前那份會一路撞限位的
> 測試韌體就是因此每趟步數一路變大。正式韌體走固定步數、歸位用慢速接近，
> 不會有這個問題。


**2026-09-17 · 輸送台實機跑合格**

- **行程 = 80 圈 = 64000 步**（800 PPR），連續四趟圈數完全相同 → 無失步
- 巡航 50us（750 rpm）穩定運轉，比預期的上限高很多
- D11 頂部、A0 底部都正確煞停，**無撞擊聲** → 限位位置有留餘裕
- DIP 改 1/4 微步（800 脈衝/圈），比原本 1/8 扭力更足

**卡住問題的真正原因：聯軸器頂絲鬆脫**

症狀是「馬達軸在轉、螺桿不動、發出咖咖聲」—— 那是頂絲在軸表面打滑，
不是失步（失步是嗡嗡聲且馬達軸也不轉）。鎖緊後完全正常。

排查時繞了遠路，因為打滑會偽裝成扭力不足：加大扭力確實有改善（撐過打滑
臨界），手推滑塊也會變順（抵銷阻力），兩者都誤導往「馬達沒力」的方向查。
之後遇到類似症狀，**先確認馬達軸與螺桿是否同步轉動**，再查其他。

> 建議在頂絲上點一滴可拆式螺絲固定膠（樂泰 243），避免震動久了又鬆開。


**2026-09-14 · 燒測試韌體實測九支腳**

- 九支輸入腳**全部正常**：接線位置正確、極性符合預期
- 六顆限位確認 NC 行為（放開 LOW、壓下 HIGH），`ACTIVE_LOW = false` 正確
- D12 / D13 兩處接點（`A.STP`/`A.DIR` 與 `SpnEn`/`SpnDir`）實測都通
- `ROWS = 7`（樣本盤實際列數）已填入

**2026-09-11 · 對照手寫筆記**

- **六顆限位全部接 `+` 端子**：X+ / Y+ / Z+ / D12 / D13 / Abort
- **DM542 DIP 開關**：`off on off off off off on on`（SW5-8 = off/off/on/on = 1600）
- `END STOPS` 旁那顆棕色電阻是 **R1**，Shield 原廠零件位，不用處理

### 測試韌體

需要重測時，`src/main.cpp` 換成獨立的輸入腳測試程式即可：關閉驅動器
（`EN=HIGH`，馬達不通電）、九支腳開內部上拉，有變化才印一行，
顯示腳位名稱與 HIGH/LOW。正式韌體先備份到專案根目錄再換。

### 極性的判斷方式

`src/main.cpp:41-42` 的說明：

- `activeLow = true` → 觸發時腳位被拉到 LOW（NO 開關接地、感測器低電位輸出）
- `activeLow = false` → 觸發時腳位是 HIGH（NC 開關斷開後被 pullup 拉高）

`CONV_TOP_ACTIVE_LOW` 那行原作者自己標記「新裝的，用 NC 要改 false」，
沒有驗證過。若接錯，回原點時抓不到頂部訊號，輸送台會一路往上撞，
直到 `MAX_STEPS_TO_TOP` 逾時才停。

---

## 八、腳位全覽

| 腳位 | 用途 | 狀態 |
|---|---|---|
| D0 / D1 | Serial（Python 通訊） | 保留 |
| D2 / D5 | X 軸 STEP / DIR | ✅ 已接 |
| D3 / D6 | Y 軸 STEP / DIR | ✅ 已接 |
| D4 / D7 | 輸送台 DM542 PUL / DIR | ⬜ 待接 |
| D8 | EN 致能 | ✅ 內建 |
| D9 | X 軸原點限位 | ⬜ 待接 |
| D10 | Y 軸原點限位 | ⬜ 待接 |
| D11 | 輸送台頂部限位（原點） | ⬜ 待接 |
| D12 | X 軸遠端限位 | ⬜ 待接 |
| D13 | Y 軸遠端限位 | ⬜ 待接 |
| A0 | 輸送台底部限位 | ⬜ 待接 |
| A1 | 樣本到位偵測 | ⬜ 待接 |
| A2 | 紅外線感測器 | ⬜ 待接 |
| A3 | 啟動開關 | ⬜ 待接 |
| A4 / A5 | （I2C 保留） | 未使用 |
